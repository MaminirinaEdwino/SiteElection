{% extends 'base.html.twig' %}

{% block title %}Candidats{% endblock %}

{% block body %}
    <h1>Candidats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidat.id }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ candidat.motif }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ candidat.photo }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ candidat.pourcentage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_candidats_index') }}">back to list</a>

    <a href="{{ path('app_candidats_edit', {'id': candidat.id}) }}">edit</a>

    {{ include('candidats/_delete_form.html.twig') }}
{% endblock %}
