<form method="post" action="{{ path('app_elections_district_delete', {'id': election.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ election.id) }}">
    <button class="btn">Delete</button>
</form>
