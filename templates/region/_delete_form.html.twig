<form method="post" action="{{ path('app_region_delete', {'id': region.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ region.id) }}">
    <button style="position:absolute; right:20px" class="btn btn-outline-danger">Delete</button>
</form>
