{% extends 'base.html.twig' %}

{% block title %}NiveauAdmin{% endblock %}

{% block body %}
    <h1>NiveauAdmin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau_admin.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ niveau_admin.niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_niveau_admin_index') }}" class="btn btn-outline-primary">back to list</a>

    <a href="{{ path('app_niveau_admin_edit', {'id': niveau_admin.id}) }}"
    class="btn btn-outline-primary">edit</a>

    {{ include('niveau_admin/_delete_form.html.twig') }}
{% endblock %}
