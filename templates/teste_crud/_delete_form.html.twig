<form method="post" action="{{ path('app_teste_crud_delete', {'id': teste_crud.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teste_crud.id) }}">
    <button class="btn">Delete</button>
</form>
