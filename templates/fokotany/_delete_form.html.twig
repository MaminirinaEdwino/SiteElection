<form method="post" action="{{ path('app_fokotany_delete', {'id': fokotany.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fokotany.id) }}">
    <button class="btn btn-outline-danger"
    style="position:absolute;
    right:20px">Delete</button>
</form>
